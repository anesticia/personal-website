import type { Metadata } from "next";
import { FieldManifoldIndex } from "@/components/field-manifold-experience";

export const metadata: Metadata = { title: "Field / Manifold Studies — Andre Huizen", description: "Four interactive research interfaces combining vector fields, curved manifolds, and real portfolio evidence." };
export default function Page() { return <FieldManifoldIndex />; }
